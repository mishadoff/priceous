version: "3.0"

services:
  priceous:
    build: ../.
    ports:
      - "8080:8080"
  solr:
    image: "solr:8.2.0"
    ports:
      - "8984:8983"
    volumes:
      - ../temp/whisky/:/var/solr/data/whisky/
